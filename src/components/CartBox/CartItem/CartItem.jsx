import React from 'react';
import * as S from './CartItem.style';

const CartItem = ({ product_id, cart_item_id, quantity, is_active }) => {
  return (
    <>
      <S.ItemContainer></S.ItemContainer>
    </>
  );
};

export default CartItem;
